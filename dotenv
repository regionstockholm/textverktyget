# Required
GEMINI_API_KEY=[gemini_api_key]
DATABASE_URL=postgresql://textverktyg:textverktyg@postgres:5432/textverktyg

# Local single-user installs may keep sample credentials.
LOCAL_DEV=true
ADMIN_API_KEY=admin
CONFIG_MASTER_KEY=[replace_with_random_secret]

# Optional local test DB
TEST_DATABASE_URL=postgresql://textverktyg:textverktyg@localhost:5432/textverktyg

# Optional managed Postgres
# DATABASE_SSL_MODE=require
